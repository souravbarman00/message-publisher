{
  "name": "message-publisher-api",
  "version": "1.0.0",
  "description": "API service for message publishing",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint . --ext .js --fix",
    "lint:check": "eslint . --ext .js",
    "lint:report": "eslint . --ext .js --format html --output-file eslint-reports/api-eslint-report.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "kafkajs": "^2.2.4",
    "aws-sdk": "^2.1467.0",
    "helmet": "^7.0.0",
    "morgan": "^1.10.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "eslint": "^8.50.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-security": "^1.7.1"
  }
}
